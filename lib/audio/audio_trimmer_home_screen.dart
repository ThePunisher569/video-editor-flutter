import 'package:flutter/material.dart';

//TODO: Add Home Screen to choose audio files from storage and edit it in app
class AudioTrimmerHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Add Audio trimming home screen'),
    );
  }
}
